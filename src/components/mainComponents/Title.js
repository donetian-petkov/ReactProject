export const Title = (props) => {

    return (
        <div className="head">
            <h3>{props.title}</h3>
            <p className="text-right"><a href="#">See all</a></p>
        </div>
    )

}
